\newcommand{\addstudent}[3]{#1 & #2 & \href{mailto:#3}{#3}\\}
\newcommand{\addtutor}[2]{#1 & #2\\}

\def\projecttitle{
    $if(projecttitle)$$projecttitle$$endif$
}

\def\students{
  \begin{tabular}{lll}
    \addstudent{Prenom}{Nom}{mail@example.com}
    %Problem comes from pandoc :
    %$for(students)$\addstudent{$if(students.name)$$students.name$$endif$}{$if(students.surname)$$students.surname$$endif$}{$if(students.mail)$\href{mailto:$students.mail$}{$students.mail$}$endif$}$endfor$
  \end{tabular}
}

\def\tutors{
  \begin{tabular}{lll}
    %$for(tutors)$\addtutor{$if(tutors.surname)$$tutors.surname$$endif$}{$if(tutors.name)$$tutors.name$$endif$}
  \end{tabular}
}
